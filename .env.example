# Compose image repository override for published deployments.
IMAGE_REPO=

# Compose image tag override for published deployments.
IMAGE_TAG=dev

# Host port mapped to frontend container port 3000.
FRONTEND_PORT=3000

# Memory limit for frontend container.
FRONTEND_MEM_LIMIT=768m
